/*
 *  UCF COP3330 Fall 2021 Application Assignment 2 Solution
 *  Copyright 2021 Joshua Davis
 */

package app;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ParseTypingTest {

    @Test
    void enforceSerialNumberTest() {
        // Pass a failing String into the method.
        // Assert that it returns "false".
        // Pass a passing String into the method.
        // Assert that it returns "true".
    }

    @Test
    void enforceNameTest() {
        // Pass a failing String into the method.
        // Assert that it returns "false".
        // Pass a passing String into the method.
        // Assert that it returns "true".
    }

    @Test
    void enforceMonetaryValueTest() {
        // Pass a failing String into the method.
        // Assert that it returns "false".
        // Pass a passing String into the method.
        // Assert that it returns "true'.
    }

    @Test
    void searchSerialTest() {
        // Create a list containing a test value.
        // Run the method by passing in that test value.
        // Test that that value returns "true".
        // Search for some other non-existent value.
        // Test that value returns "false".
    }

    @Test
    void searchNameTest() {
        // Create a list containing a test value.
        // Run the method by passing in that test value.
        // Test that that value returns "true".
        // Search for some other non-existent value.
        // Test that value returns "false".
    }
}